// Fill out your copyright notice in the Description page of Project Settings.

#include "MeleeAbility.h"


void AMeleeAbility::ChargeAbility(ABaseCharacter * user, FVector direction, float range)
{
	if (user != nullptr)
	{

	}
}

void AMeleeAbility::ExecuteAbility(ABaseCharacter * user, FVector direction, float range)
{
	if (user != nullptr)
	{
		FVector startPoint = user->GetActorLocation();
		

	}
}
